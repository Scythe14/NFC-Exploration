#include <stdlib.h>
#include <string.h>
#include <nfc/nfc.h>
#include <toolh.h>

void		detect_tags(MifareTag *tags)
{
  int		i;

  i = 0;
  while (tag[i]) {

    ++i;
  }
}

int		init_nfc_context()
{
    if (context == NULL)
    {
      return (FAIL)
    }
    return (0);
}

int		main(int ac, char **av)
{
  nfc_context	*context;
  nfc_device	*pnd;
  MifareTag	*tags;
  int		i;


  if (init_nfc_context() == FAIL)
    return (FAIL);
  tags = freefare_get_tags(pnd);
  detect_tags(tags);

  nfc_close(pnd);
  nfc_exit(context);
  return (0);
}
